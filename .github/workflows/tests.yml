name: 🧪 Run Search Tests

on:
  workflow_dispatch:  # Запуск вручную из Actions

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v3

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Install dependencies
        run: npm install

      - name: ▶️ Run tests with Mocha from search-test
        run: npx mocha search-test/*.js --timeout 30000 --parallel --reporter spec > test-report.log || true
        
      - name: 📤 Upload plain text test report
        uses: actions/upload-artifact@v4
        with:
          name: text-report
          path: test-report.log
