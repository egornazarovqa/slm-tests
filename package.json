{
  "name": "selenium",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "mocha search-test/*.js --timeout 30000 --no-timeouts --parallel --reporter mochawesome --require mochawesome/register --reporter-options reportDir=search-test/mochawesome-report,reportFilename=report"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "mocha": "^11.7.1",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.34.0"
  }
}
